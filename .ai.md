# Claude Gate - Project Conventions

## Overview
Claude Gate는 개인 Claude 구독의 OAuth 토큰을 여러 Agent/Claude Code 인스턴스에서 안전하게 나눠 쓸 수 있게 해주는 reverse proxy.

## Tech Stack
- Go 1.23+
- SQLite (modernc.org/sqlite, pure Go, no CGO)
- HTMX + Go html/template (Web UI)
- GitHub Actions (CI/CD)

## Code Style
- `gofmt -s` 로 포맷팅
- `go vet` 통과 필수
- `golangci-lint` 통과 필수
- 에러는 `fmt.Errorf("context: %w", err)` 형태로 wrapping
- 테스트 파일은 같은 패키지 내 `_test.go`
- 주석은 필요한 경우에만, godoc 스타일

## Project Structure
```
cmd/claude-gate/    → 엔트리포인트
internal/config/    → 환경변수 기반 설정
internal/proxy/     → reverse proxy + SSE tapping
internal/token/     → 토큰 풀 관리 (round-robin, sticky)
internal/usage/     → 사용량 추적
internal/admin/     → Admin REST API handlers
internal/store/     → SQLite repository layer
migrations/         → SQL migration files
web/                → embedded web UI (templates + static)
testutil/           → 테스트 헬퍼
```

## Database
- SQLite WAL 모드 사용
- Read/Write 커넥션 분리 (write: MaxOpenConns=1, read: MaxOpenConns=N)
- 마이그레이션은 `migrations/` 디렉토리의 번호 순서 SQL 파일
- `modernc.org/sqlite` 사용 (CGO 불필요)

## Testing
- 모든 엔드포인트 최소 1개 테스트
- `testutil.NewTestDB()` 로 in-memory SQLite 사용
- `testutil.NewMockUpstream()` 으로 Claude API mock
- `go test -race` 필수

## Naming
- gate token: `gate-` 접두사 + base62 32자
- 환경변수: `CLAUDE_GATE_` 접두사
- Admin API: `/admin/api/` 접두사
- Proxy: `/admin/` 이외 모든 경로

## Dependencies
- 최소화 원칙: stdlib 우선
- HTTP: `net/http`, `net/http/httputil`
- Router: `http.ServeMux` (Go 1.22+ method routing)
- JSON: `encoding/json`
- 외부: `modernc.org/sqlite` only
